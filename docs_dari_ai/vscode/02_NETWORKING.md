# Networking - dasar-python

Project ini join 1 network shared:

- `proxy-network` untuk akses `mysql-main` dan `redis-main`

Catatan:
- container `mysql-check` / `mysql-init` juga join `proxy-network` karena perlu akses `mysql-main`.

